package enthuware;

import java.io.FileNotFoundException;
import java.io.IOException;
import java.util.ArrayList;

import static enthuware.PrintScreen.*;

public class Enthuware_43 extends PrintScreen {
	static int ix;
	enum Color{Blue, yellow, red, white, purple}

    public static void main (String[] args) throws Exception {

    		"string".equals(null);

    		String str = null;
    		switch(str){

    		}
    	
        	psln("Color.Blue: " + Color.Blue);
        	
        	psln("Color.Blue.equals(Blue): " + Color.Blue.toString().contains("Blue"));	// true
        	psln("Color.Blue.equals(Blue): " + Color.Blue.toString().equals("Blue"));	// true
	}
}
